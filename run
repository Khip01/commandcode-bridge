#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")"

if [ ! -f "bin/commandcode_bridge" ]; then
  echo "Binary not found. Building first..."
  ./build
fi

if [ "${1:-}" = "server" ]; then
  shift
  exec bin/commandcode_bridge run --server "$@"
else
  exec bin/commandcode_bridge run "$@"
fi
